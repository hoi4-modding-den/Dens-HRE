on_actions = {
	on_startup = {
		effect = {
			ZZZ = { HRE_title_system = yes } #tag is arbitrary but required, as the effect runs on every country anyway
			ZZZ = { HRE_title_system_funky_state = yes } #tag is arbitrary but required, as the effect runs on every country anyway
		}
	}
}