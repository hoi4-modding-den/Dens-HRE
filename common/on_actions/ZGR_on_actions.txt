on_actions = {


on_startup = {
		effect = {
			ZGR = {
                set_variable = {
                    var = civil_war
                    value = 60
                }
			}
		}
}


on_daily_ZGR = {
        effect = {
            # If democratt < 0.5, decrease variable but not below 0
            if = {
                limit = {
                    democratic < 0.5
                    check_variable = { civil_war > 0 }   # only if above 0
                }
                add_to_variable = { var = civil_war value = -0.1 }
            }

            # If democratt > 0.5, increase variable but not above 100
            if = {
                limit = {
                    democratic > 0.5
                    check_variable = { civil_war < 100 }  # only if below 100
                }
                add_to_variable = { var = civil_war value = 0.1 }
            }

            if = {
                limit = {
                    check_variable = { civil_war > 100 }
                }
                clamp_variable = {
    			var = civil_war
    			min = 0
    			max = 100
		}
            }
        }
}




}