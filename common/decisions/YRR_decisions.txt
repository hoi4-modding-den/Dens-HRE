political_actions = {

    YRR_utilise_the_falangists = {

        icon = eng_blackshirt_march

        cost = 50

        priority = 2

        ai_will_do = {
            base = 10
        }

        allowed = {
            original_tag = YRR
        }

        available = {
            has_character = YBS_julio_ruiz_de_alda
            communism < 0.1
            fascism > 0.2
        }

        visible = {
            has_idea = YRR_anarchist_resistance
        }

        modifier = {
            political_power_cost = 0.1
        }

        days_remove = 200

        remove_effect = {
            remove_ideas = YRR_anarchist_resistance
        }
    }

    YRR_reign_in_the_army = {

        icon = oppression

        cost = 50

        priority = 2

        ai_will_do = {
            base = 10
        }

        allowed = {
            original_tag = YRR
        }

        available = {
            has_country_flag = YRR_power_struggle_over
        }

        visible = {
            has_idea = YRR_army_state
        }

        modifier = {
            experience_gain_army_factor = -0.25
        }

        days_remove = 200

        remove_effect = {
            remove_ideas = YRR_army_state
        }
    }

    YRR_power_struggle_mission = {

        icon = generic_political_discourse

        allowed = {
            original_tag = YRR
        }

        activation = {
            has_character = YBS_julio_ruiz_de_alda
            has_government = neutrality
            NOT = { has_country_flag = YRR_power_struggle_over }
        }

        available = {
            NOT = { has_government = neutrality }
        }

        is_good = yes

        days_mission_timeout = 300

        timeout_effect = {
            country_event = YRR_events.1
            set_country_flag = YRR_power_struggle_over
        }

        complete_effect = {
            set_country_flag = YRR_power_struggle_over
        }

        cancel_effect = {
            set_country_flag = YRR_power_struggle_over
        }
    }

}

foreign_politics = {

    YRR_attack_YBS = {

        icon = generic_prepare_civil_war

        priority = -98

        cost = 100

        ai_will_do = {
            base = 10
            modifier = {
                factor = 0
                has_war = yes
            }
        }

        allowed = {
            original_tag = YRR
        }

        available = {
            date > 1939.1.1
			is_subject = no
            NOT = { is_ally_with = YBS }
        }

        visible = {
            country_exists = YBS
            NOT = { has_war_with = YBS }
        }

        cancel_trigger = {
            OR = {
                is_subject = yes
                is_ally_with = YBS
                has_war_with = YBS
                NOT = { country_exists = YBS }
            }
        }

        days_remove = 50

        complete_effect = {
            YBS = {
                country_event = YBS_diplo_events.5
            }
        }

        war_with_on_remove = YBS

        remove_effect = {
            declare_war_on = {
                target = YBS
                type = civil_war
            }
        }
    }

    YRR_begin_full_offensive = {

        icon = generic_nationalism

        priority = -99

        cost = 100

        fire_only_once = yes

        ai_will_do = {
            base = 10
        }

        allowed = {
            original_tag = YRR
        }

        available = {
            is_subject = no
            NOT = { country_exists = YBS }
        }

        complete_effect = {
            custom_effect_tooltip = YBS_lobby_for_intervention_tt
			set_country_flag = YBS_seeds_of_discord_unlocked

            custom_effect_tooltip = generic_skip_one_line_tt

			every_state = {
				limit = {
					YBS_is_iberian_state = yes
					NOT = { is_core_of = ROOT }
					NOT = { is_claimed_by = ROOT }
				}
				add_claim_by = ROOT
			}
        }
    }

    YRR_reunify_spain = {

        icon = generic_form_nation

        priority = -100

        ai_will_do = {
            base = 10
        }

        allowed = {
            original_tag = YRR
        }

        available = {
            YBS_owns_all_iberian_states = yes
        }

        fire_only_once = yes

        complete_effect = {
            set_global_flag = YBS_iberian_civil_war_ended
            set_cosmetic_tag = YRR_spain

            if = {
                limit = {
                    NOT = { has_government = fascism }
                }
                custom_effect_tooltip = generic_skip_one_line_tt
                hidden_effect = {
                    YKP = {
                        every_core_state = {
                            remove_core_of = YKP
                        }
                    }
                    180 = { add_core_of = YKP }
                    181 = { add_core_of = YKP }
                    112 = { add_core_of = YKP }
                    795 = { add_core_of = YKP }
                    179 = { add_core_of = YKP }
                }
                release_puppet = YKP
            }

            custom_effect_tooltip = generic_skip_one_line_tt

            every_state = {
				limit = {
					YBS_is_iberian_state = yes
					NOT = { is_core_of = ROOT }
				}
				add_core_of = ROOT
			}

            if = {
                limit = {
                    has_idea = YRR_low_legitimacy
                }
                custom_effect_tooltip = generic_skip_one_line_tt
                remove_ideas = YRR_low_legitimacy
            }
            if = {
                limit = {
                    has_idea = YRR_low_legitimacy_2
                }
                custom_effect_tooltip = generic_skip_one_line_tt
                remove_ideas = YRR_low_legitimacy_2
            }

            if = {
                limit = {
                    has_government = fascim
                }
                custom_effect_tooltip = generic_skip_one_line_tt
                add_ideas = YRR_spanish_destiny
            }
        }
    }

}
